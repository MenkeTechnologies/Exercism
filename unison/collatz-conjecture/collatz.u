use Nat / increment
steps : Nat -> Optional Nat
steps n =
  next n = if (mod n 2) === 0 then n / 2 else (3 * n) + 1
  go count = cases
     0 -> None
     1 -> Some count
     n -> go (increment count) (next n)
  go 0 n
