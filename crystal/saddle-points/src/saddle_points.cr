module SaddlePoints
  def self.saddle_points(matrix : Array(Array(Int32))) : Array(NamedTuple(row: Int32, column: Int32))
    # Write your code for the 'SaddlePoints' exercise in this file.
  end
end
