import Extra

/-
  Implement syntax here for a DSL similar to the DOT language, following the instructions.

  Note that nodes in an edge are separated by a single `-`, not `--`.
  This avoids conflicts with Lean's single-line comment syntax.

  Every statement ends with `;`, even graph-level comments.

  For more details, refer to the test file.
-/
